import { environment } from 'src/environments/environment';

export const API_URL = environment.API_URL;
export const AI_TOKEN = environment.AI_TOKEN;
export const SERVER_URL = environment.SERVER_URL;
